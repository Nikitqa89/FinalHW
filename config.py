"""
Конфигурация проекта Telegram-бота на базе трансформера.

Здесь хранятся все гиперпараметры и флаги, связанные с:
- обучением модели
- архитектурой трансформера
- генерацией текста
- предобработкой данных
"""

import os

# ======================
# 🔧 Параметры обучения
# ======================

batch_size = 128           # Размер пакета (batch) для обучения
block_size = 128           # Длина входной последовательности (контекст)
max_iters = 2500          # Максимальное количество итераций обучения
eval_interval = 500        # Как часто печатать метрику (итераций)
eval_iters = 200           # Сколько батчей использовать для оценки
learning_rate = 2e-4       # Скорость обучения

# ============================
# ⚙️ Устройство (GPU/CPU)
# ============================

# Выбор устройства для PyTorch (используем GPU, если доступен)
device = 'cuda' if __import__('torch').cuda.is_available() else 'cpu'

# ==============================
# 🧠 Параметры трансформера
# ==============================

n_embd = 384               # Размерность эмбеддинга (token и position)
n_head = 6                 # Кол-во голов в Multi-Head Attention
n_layer = 8                # Кол-во блоков трансформера
dropout = 0.3              # Dropout для регуляризации

# ======================
# 📄 Работа с данными
# ======================

# Абсолютный путь к исходному файлу данных (надежно работает из любой директории)
data_path = os.path.join(os.path.dirname(__file__), "transformer_model", "data.txt")

# Удалять ли заголовки в preprocess (всё заглавными, ≤7 слов и без пунктуации)
remove_titles = True

# Загрузить "best_model.pt" или "model.pt"?
load_best_model = True